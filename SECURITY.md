# Security policy

Do not open a public issue for a vulnerability involving tokens, recipient data, unsubscribe links, provider credentials or production endpoints.

Report sensitive findings through GitHub's private vulnerability reporting for this repository. Include the affected file, reproduction steps, impact and a proposed mitigation when possible.

Never include real subscriber data, API keys, `.env` files or signed resource URLs in a report.
